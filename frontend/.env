# Backend origin for the Vite dev server /api proxy (vite.config.ts)
VITE_API_PROXY_TARGET=http://localhost:8080

# Base URL for API calls from the app (e.g. axios). Use /api in dev with the proxy.
VITE_API_BASE=/api
